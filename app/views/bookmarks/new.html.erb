<div class="container pt-3">
  <h1>Add movie to <%= @list.name %> list</h1>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.submit "Add movie" %>
  <% end %> 
</div>