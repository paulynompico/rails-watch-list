<h1>Movie List</h1>

<div class="container">
  <% @movies.each do |movie| %>
    <div class="card">
      <img src=<%=movie.poster_url%> alt="movie poster">
      <p><%= movie.title %></p>
    </div>
  <% end %>
</div>